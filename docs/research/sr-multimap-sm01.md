# SM01 — S/R strategies across local, four-hour and daily maps

Frozen September 17, 2026 before new economic outcomes. Local research only.

## Selection before execution

| Prior action | Evidence on the local 14d map | This pass |
|---|---|---|
| Live profitable resistance partial with deteriorating pulse | SRP01 beat disabled in all four Agg10 cases; SRL01 removing resistance lost net in all four | Test unchanged action on each map set |
| Buffered resistance ordinary TP plus current partials | SRT03 net +$2,422/+4,026 recent and +$440/+7,436 longer, but resting DD/monthly failures | Test as the strongest unqualified net lead |
| Exact resistance TP | Worse recent and older resting results than buffered | Park |
| S/R-selected fixed1% TP | Mostly shares buffered mechanism; weaker net in all four comparisons | Existing diagnostic control, not duplicated here |
| Global1% TP | Not a level-map strategy; loses recent net | Park |
| Selected-TP wait/breakout release | SRR01 failed cross-period; proposed re-entry mechanism not supported | Park |
| Timer add skip, support boosts/reopen, major fresh-ladder blocks,6h flatten | Failed/low reach, or support-reopen inactive under Agg10 | Park; no silent activation |

Neither a profitable recent number nor this selection erases prior screen failures.

## Matrix

Run **partial only** and **partial + buffered ordinary TP** independently with:

1. Local14d only — exact existing strategy controls.
2. Major4H only — local14d S/R actions removed/replaced.
3. Major1D only — local14d S/R actions removed/replaced.
4. Local14d + major4H.
5. Local14d + major1D.
6. All three.

Also show unchanged Agg10 with no S/R actions (SRP01 partial-disabled control).
This is the five requested alternative map sets plus local baseline, not an
extra 4H+1D-only cell or an AND/OR parameter sweep.

Combining means **any healthy enabled map may qualify an action**, not confluence
and not multiple simultaneous exits. A partial uses the nearest role-valid centre
above price, its original0.3% action buffer and a shared60m cooldown. For shading,
test each map's nearest resistance separately against the original band/three-touch
rule; choose the nearest qualified source. A nonqualifying nearest level cannot
be skipped for a farther level from that same map. Touches are never pooled across
maps. Equal price ties prefer local14d,4H,1D in that order.

Macro roles come from confirmed historical flips; a zone marked support is not
invented as resistance merely because price moves below it intraday. Use saved
centres for price rules, not a freshly selected zone edge. Local14d retains its
original price-relative sides. That semantic difference is part of comparing
the already-built maps, not a pure timeframe-only experiment.

## Rules retained

Partial: depth>=6, keep three worst rungs by raw dollar PnL, gross ladder PnL
>=0.25%, selected plan profitable after fees, deteriorating pulse, resistance
<=0.3% above current price,60m cooldown. Original allocation, surviving-rung age,
last-add clock and next-open execution stay unchanged.

Buffered TP: ordinary1.4% phase only; nearest resistance within1% of price,
in [average*1.01, ordinary target), three confirmed touches spaced>=6h.
Set max(average*1.01,resistance*0.997), held until inventory or phase changes.
No same-bar high fills, no raising stale0.5%, no superseding another pending
exit/partial/add. Existing Agg1010h/48h-high behavior and gates remain fixed.

Macro qualification uses the existing map definitions; the shade's three-touch
requirement remains an additional action gate (not relaxed to create more daily
signals). Missing/unhealthy sources contribute no levels, not fabricated ones.

## Windows, controls and accounting

Recent: May17,2026 20:43–September15,2026 20:20 UTC.
Longer: July1,2025 00:00–August19,2026 21:32 UTC.
$32,000 flat start, $800x1.35/max11,0.055% entry and exit fees. Both resting-touch
and close-confirmed/next-open TP assumptions. No current-wallet or maker claim.

**72 paths:**12 exact archive controls first,40 new primary paths,20 recent
geometry+60s sensitivities. Ten new map/action definitions; not72 independent
strategies. Source-delay cases move only S/R geometry, not pulse or other gates.
Every path has its own inventory, occupancy, replacement ladders and open mark.

Reuse immutable4H/daily maps. Save a local14d snapshot tape once, with the exact
off-boundary initial rebuilds needed for both window starts/source lags. Reuse
that tape in all paths. A study-owned engine copy adds only a context-provider
factory; the canonical source and production files remain untouched.

Controls require exact engine digest, metrics and config hashes before any new
strategy path. The independent checker reconstructs fills/fees/equity and target,
partial and action priority minute by minute, not just sampled fired events.
Known-state dates/roles/touches must never come from the final map.

## Reports and qualification

Always show current Agg10/local14d and no-S/R-action reference. For each strategy
also show its local14d control and matching-map partial-only comparison. Report
W/L counts/dollars, average loss, realized plus remaining mark, maxDD, TPs and
partials, per-month MTM and W/L, action/source counts, changed episodes, and
concentration in largest replacement sequences. Sparse daily cases stay explicit.

Existing screen remains: recent net>=+$1,000, longer>=0, no month worse than
-$250, no DD increase,>=20 changed episodes, delayed net>=0. Qualify against
unchanged current Agg10; show matched-strategy deltas separately. No qualifying
result means no upgrade. Even a pass is research, not deployment authority.

Earlier data has incomplete HL coverage and explicit BTC/funding fallbacks.
These overlapping, heavily mined windows are not holdouts. No fee rebate,
funding cash, margin/liquidation, maker queue or original transport latency claim.
