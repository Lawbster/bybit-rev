# SRL01: resistance's incremental contribution to the pulse partial

Frozen September 16, 2026 before results. Local research only.

One alternative: unchanged Agg10 versus identical partial without level presence
or proximity. Retain all price/profit/depth/keep3/pulse/cooldown/context gates,
mutation priority, next-open fill and preserved inventory clocks. No placebo or
other map, trigger threshold, exit, entry, size, fee or production changes.

SRP01 disabled is **no partial action**, not an always-on partial with neither
filter. This does not complete a 2x2. Archived resistance-only and disabled
references remain labeled; the neither-filter action is not newly tested.

Ten exact archived controls precede six alternative paths: eight SRP01 B17/Agg10
controls plus two recent current-pulse60 controls; B across two windows/two TP
models and two recent B-pulse60 sensitivities. One new definition,16 runs.
Same May17 20:43-Sep15 20:20,2026 / Jul1,2025-Aug19 21:32,2026 UTC windows,
flat$32k,$800x1.35,max11,0.055% fees each side. No maker or funding cash uplift.
Not independent holdouts or a current-wallet liquidation model.

The research adapter rebuilds the same private shadow inventory plan and checks
exact equality whenever the shadow provides one. Presence/proximity are removed
both from candidate construction and action gates; healthy14d candle coverage
remains mandatory. The current-mode adapter must reproduce archived digests
and metrics exactly. Default engine behavior remains unchanged. Invalid/combined
research adapters fail explicitly. No src/bot file is edited.

All otherwise eligible non-level/non-pulse decision minutes are journaled,
including no resistance and rejected pulse. Selection is by current dollar PnL,
not simply highest entry. No future fill price selects the rungs. Pulse60 delays
all partial pulse operands including historical BTC/funding fallback, not price,
other gates or fills. Missing pulse remains unknown, not adverse by fiat.

The independent checker reconstructs every eligible minute, selected IDs, pulse,
map, intervention, ledger, fee, target, high exit and monthly mark; full prefixes
and future-poison tests supplement source pins. Use the SRP01 corrected saved-
target audit, not the original check expecting unpersisted controller counters.

Report W/L counts/dollars/average loss,monthly MTM/DD, ending inventory, partial
counts and eligible denominators, replacement cycles and concentration. A win
does not prove every extra partial was good; ownership path changes are included.
Keep the existing screen: recent >=$1k both models, longer >=0, no DD increase,
no month <-$250 delta, >=20 changed recent ladders, delayed net>=0 with the
same monthly/DD checks. Qualification is not deployment;30-60d forward required.

```powershell
npx ts-node scripts/sr-level-ablation-tests.ts
npx ts-node scripts/sr-partial-audit-tests.ts
npx ts-node scripts/replay-current-stack-tests.ts
npx ts-node scripts/sr-level-ablation-study.ts plan
npx ts-node scripts/sr-level-ablation-study.ts run KEY
npx ts-node scripts/sr-level-ablation-verify.ts KEY
npx ts-node scripts/sr-level-ablation-report.ts KEY
npm run research:workflow -- verify KEY
```

Do not sync datasets during the job. Accepted output directories are immutable.
The engine-only source bridge is explicit; all other inherited source pins,
archive artifacts and protected production files are verified before/after.
